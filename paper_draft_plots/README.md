# paper_draft_plots

Figures for [reports/PAPER_DRAFT.md](../reports/PAPER_DRAFT.md), generated by [make_figures.py](make_figures.py).

Each figure is written at **300 dpi** in two formats, sized for a journal column/full-width layout:

- **`.pdf`** — vector, for LaTeX submission (`\includegraphics`).
- **`.png`** — raster, used by the Markdown draft preview.

| File | Figure in draft | Content |
|---|---|---|
| `fig1_motivation` | Fig 1 | Monitoring-density gap + LSTM-TL vs graph-TL motivation |
| `fig2_city_graphs` | Fig 2 | k-NN station graphs (real coords) for Delhi/Kolkata/Guwahati |
| `fig3_encoder_blocks` | Fig 3 | Inductive ST-GNN encoder block diagram with tensor shapes |
| `fig4_graph_dann` | Fig 4 | Graph-DANN architecture + λ(p) warm-up inset |
| `fig5_stage1_heatmaps` | Fig 5 | Stage-1 three-way verification small multiples |
| `fig6_headline_bars` | Fig 6 | Cross-method transfer R² at d = 30 % |

Regenerate all figures:

```bash
python paper_draft_plots/make_figures.py
```

Figure 2 reads real station coordinates from `dataset/processed/metadata.json`; the others are self-contained (data hard-coded from the final-protocol results). Edit the data arrays or styling at the top of `make_figures.py` and re-run.
